{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Deliberately no "chrome" here. This package is consumed by the extension,
    // the orchestrator and the dashboard; a chrome.* reference must not compile.
    "types": [],
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  },
  "include": ["src"]
}
